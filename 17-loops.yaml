- name: loops 
  hosts: local 
  connection: local 
  tasks: 
    - name: print names
      ansible.builtin.debug: 
       msg: " Hello... {{ item }}" # items here is reserved keyword
      loop :
       - jyothi 
       - sneha
       - suvarsha 
